name: CI Pipeline

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 20
        uses: actions/setup-java@v3
        with:
          java-version: '20'  # Updated to match the mentioned JDK version
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - name: Run Cucumber Tests
        run: mvn clean test

      - name: Generate Cucumber HTML Report
        run: mvn verify

      - name: Upload Cucumber HTML Report
        uses: actions/upload-artifact@v4
        with:
      - name: cucumber-html-report
        path: target/cucumber-report-html

#      - name: Run Unit Tests
#        run: mvn test
#
#      - name: Run Integration Tests
#        run: mvn test
#
#      - name: Upload Test Report
#        uses: actions/upload-artifact@v3  # Ensure you are using a compatible version (v3 here)
#        with:
#          name: test-report
#          path: target/surefire-reports  # Assuming the Maven surefire plugin generates the test reports here
